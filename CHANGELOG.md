<!-- 本文件由 tools/update-changelog.ps1 自动生成，内容来自 src/PrinterShareFixer.Core/AppInfo.cs -->

# 更新日志

## [1.3.0] - 2026-09-25

- 新增：应用内自动更新。在「设置」里点「检查更新」→ 显示新版本更新内容 → 点「立即更新并重启」，程序会自动下载、校验、替换并重新启动。
- 更新包来源为 GitHub Releases：读取最新版本号，按当前安装形态（自带运行时 / 精简版）自动选择对应压缩包。
- 用 Release 附带的 SHA-256 校验下载结果，校验不通过会自动删除并报错，不会替换程序。
- 替换策略：先把新版本复制到同级目录，旧版本改名备份（可回滚），切换失败自动还原；全过程写入日志，失败弹窗提示。
- 更新过程不动用户数据——日志与注册表备份都在 ProgramData 下，替换程序目录不会丢。
- 启动时每天最多静默检查一次更新，发现新版本会在「设置」按钮上显示提示点；网络不通则静默忽略。
- 新增：psfix update [--stage] 命令行检查更新；--stage 可把更新包下载并解压到临时目录用于验证。
- 发布策略调整：发行版只提供「不自带运行时」的精简包（体积约减少 54%）。首次使用需安装 .NET 10 运行时，包内「启动程序.bat」会在缺少运行时时用中文提示并打开下载页。
- 注意：从 v1.2.0 的「自带运行时」包升级到本版本后，需要先安装 .NET 10 运行时才能启动。

## [1.2.0] - 2026-09-25

- 新增：不自带运行时的版本（framework dependent），体积更小，首次运行前需要安装 .NET 10 桌面运行时，包内附安装指引与下载链接。
- 仓库结构按 GitHub 约定整理：新增 LICENSE、CHANGELOG.md、docs/ 文档目录与 .github/ 工作流，发布包改由 GitHub Releases 提供。
- 新增：GitHub Actions 自动构建（推送到 main 或提交 PR 时编译并产出两种发布包）。
- 新增：psfix version --markdown 与 tools/update-changelog.ps1，用同一份更新内容生成仓库的 CHANGELOG.md。

## [1.1.2] - 2026-09-25

- 修复：防火墙多端口规则写法错误（“137,139”会被系统判为无效端口），导致 NetBIOS / WSD 放行规则没能创建，现改为逐个端口传入。
- 修复：NetBIOS over TCP/IP 设置失败——较新系统上 CIM 对象没有 SetTcpipNetbios 方法，现改用 Invoke-CimMethod，并增加直接写 NetBT 注册表的回退方案。
- 修复：本机没有打印机时“确认已共享打印机”一步因脚本中的中文引号解析失败而报错（PowerShell 会把 “ ” 当成字符串定界符），现已修正。
- 修复：注册表备份步骤里 reg.exe 的中文报错显示为乱码，现在不存在的注册表项直接跳过，不再调用 reg.exe。
- 修复：Windows 11 上把系统显示成“Windows 10 Pro 25H2”，现在按内部版本号正确显示为 Windows 11。
- 优化：“网络发现”规则组在部分系统上找不到，现在只作为提醒，不再把整个防火墙步骤判为失败。
- 优化：检测与复核不再全量枚举防火墙规则，防火墙查询从约 11 秒降到 1-2 秒。
- 新增：脚本执行失败时把出错的脚本内容写入日志，便于定位问题。
- 新增：tools/check-scripts.ps1，提交前可自检所有内嵌 PowerShell 脚本的语法。

## [1.1.1] - 2026-09-25

- 修复：点击“开始修复”后窗口卡死、进度不刷新、取消按钮失效——原因是修复步骤在界面线程上同步等待，现已全部改到后台线程执行。
- 修复：关闭窗口后进程仍在后台运行、只能用任务管理器结束；现在关闭窗口会立即取消正在执行的修复并退出进程。
- 修复：启动修复时不再重复读取整份系统状态快照（原先最多要读 4 次），系统版本改为直接读注册表，等待时间明显缩短。
- 新增：日志记录每个步骤的耗时，便于定位卡在哪一步。
- 新增：单条系统查询最长等待 45 秒，避免个别卡住的系统组件把流程拖死。

## [1.1.0] - 2026-09-25

- 界面按角色拆成两个：本机接有打印机（开放共享）、本机没有打印机（连接共享）。
- 新增“设置”对话框：查看版本号、每个版本的更新内容、日志与备份目录。
- 客户端模式新增目标电脑连通性测试：ping、TCP 445、TCP 135、net view。
- 客户端模式新增清理失效打印缓存与卡住的打印队列（打印任务卡死、0x0000007c 一类问题）。
- 客户端模式新增 AllowInsecureGuestAuth 策略设置，解决 Windows 11 24H2 访问无密码共享被拒。
- 服务清单按角色拆分：服务端保留 Server/网络发现，客户端只保留连接所需服务。

## [1.0.0] - 2026-09-24

- 首个版本：Windows 10 / Windows 11 两套一键修复方案。
- 服务配置、网络位置、NetBIOS、防火墙放行、来宾访问、SMB 兼容性。
- 修复 0x0000011b（打印 RPC 隐私认证）与 0x00000740（驱动安装限制）。
- Windows 11 24H2 专项：打印后台处理程序 RPC 传输、受保护的打印模式。
- 系统状态检测、逐项进度显示、修复前注册表备份与详细日志。
- 附带命令行工具 psfix 与应用图标。

