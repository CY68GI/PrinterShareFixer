<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PrinterShareFixer.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="打印机共享修复工具">

    <NavigationView
        x:Name="Nav"
        PaneDisplayMode="Left"
        IsSettingsVisible="True"
        IsBackButtonVisible="Collapsed"
        IsPaneToggleButtonVisible="True"
        OpenPaneLength="236"
        SelectionChanged="OnNavSelectionChanged"
        Loaded="OnNavLoaded">

        <NavigationView.MenuItems>
            <NavigationViewItem Content="修复打印机共享" Tag="repair">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE90F;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="打印机信息" Tag="printers">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE749;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 顶部：图标、标题、当前机器信息、权限状态（所有页面共用） -->
            <StackPanel Grid.Row="0" Padding="24,16,24,8" Spacing="10">
                <Grid ColumnSpacing="14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image
                        x:Name="HeaderIcon"
                        Grid.Column="0"
                        Width="44"
                        Height="44"
                        VerticalAlignment="Top" />
                    <StackPanel Grid.Column="1" Spacing="4">
                        <TextBlock Text="打印机共享修复工具" Style="{StaticResource TitleTextBlockStyle}" />
                        <TextBlock
                            x:Name="SubtitleText"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            TextWrapping="Wrap"
                            Text="正在读取系统信息…" />
                    </StackPanel>
                </Grid>
                <InfoBar
                    x:Name="AdminInfoBar"
                    IsOpen="True"
                    IsClosable="False"
                    Severity="Informational"
                    Title="正在检测…"
                    Message="" />
            </StackPanel>

            <Frame x:Name="ContentFrame" Grid.Row="1" />
        </Grid>
    </NavigationView>
</Window>
